# Work Log Index

This folder stores timestamped execution logs for Codex prompts.

Latest pointer:
- `LATEST.md`

Naming convention:

```text
WORK_LOG_<YYYYMMDD_HHMMSS>.md
```

Each work log should record what was inspected, changed, intentionally not changed, and recommended next steps.

