# Data Scripts

This folder is reserved for future organized data scripts.

Current historical/root-level data scripts are preserved at:

- `scripts/prepare_manifests.py`
- `scripts/prepare_folds.py`
- `scripts/download_datasets.py`

Do not move them until imports, README commands, and tests are updated in a dedicated refactor.

